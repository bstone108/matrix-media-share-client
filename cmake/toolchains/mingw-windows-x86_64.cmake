set(CMAKE_SYSTEM_NAME Windows)
set(CMAKE_SYSTEM_PROCESSOR x86_64)

if(DEFINED MATRIX_MEDIA_ARCHIVER_MINGW_PREFIX)
    set(MINGW_PREFIX "${MATRIX_MEDIA_ARCHIVER_MINGW_PREFIX}")
elseif(DEFINED ENV{MATRIX_MEDIA_ARCHIVER_MINGW_PREFIX})
    set(MINGW_PREFIX "$ENV{MATRIX_MEDIA_ARCHIVER_MINGW_PREFIX}")
elseif(APPLE)
    set(MINGW_PREFIX "/opt/homebrew/bin/x86_64-w64-mingw32")
else()
    set(MINGW_PREFIX "/usr/bin/x86_64-w64-mingw32")
endif()

set(CMAKE_C_COMPILER "${MINGW_PREFIX}-gcc")
set(CMAKE_CXX_COMPILER "${MINGW_PREFIX}-g++")
set(CMAKE_RC_COMPILER "${MINGW_PREFIX}-windres")
set(CMAKE_AR "${MINGW_PREFIX}-ar")
set(CMAKE_RANLIB "${MINGW_PREFIX}-ranlib")
set(CMAKE_STRIP "${MINGW_PREFIX}-strip")

# Keep host tools discoverable while target libraries and packages come from the Qt SDK root.
set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
set(CMAKE_FIND_ROOT_PATH_MODE_PACKAGE ONLY)
